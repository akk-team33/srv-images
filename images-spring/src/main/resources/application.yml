server:
  port: 2080
images:
  entries:
    - alias: cwd
      path: .
      order: NAME # PATH, NAME, UPDATE
      direction: ASC # ASC, DESC
    - alias: root
      path: /
      order: PATH # PATH, NAME, UPDATE
      direction: ASC # ASC, DESC
